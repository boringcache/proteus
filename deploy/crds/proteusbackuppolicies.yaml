apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: proteusbackuppolicies.proteus.io
spec:
  group: proteus.io
  names:
    categories: []
    kind: ProteusBackupPolicy
    plural: proteusbackuppolicies
    shortNames:
    - pbackuppolicy
    singular: proteusbackuppolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repositoryRef
      name: Repository
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.nextRunAt
      name: Next
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ProteusBackupPolicySpec via `CustomResource`
        properties:
          spec:
            properties:
              includeClusterResources:
                default: false
                type: boolean
              includeVolumes:
                default: true
                type: boolean
              labelSelector:
                nullable: true
                type: string
              paused:
                default: false
                description: When true, scheduled ticks do not create runs (manual Run now still allowed).
                type: boolean
              pvcNames:
                description: PVCs to back up, by name, in `target_namespace`. Required, at least one.
                items:
                  type: string
                minItems: 1
                type: array
              repositoryNamespace:
                nullable: true
                type: string
              repositoryRef:
                type: string
              retention:
                default:
                  keepLast: 7
                properties:
                  keepLast:
                    default: 7
                    format: uint32
                    minimum: 0.0
                    type: integer
                  maxAgeDays:
                    format: uint32
                    minimum: 0.0
                    nullable: true
                    type: integer
                type: object
              schedule:
                description: 5-field crontab (`min hour dom month dow`) or 6-field with seconds; UTC.
                nullable: true
                type: string
              targetNamespace:
                type: string
            required:
            - pvcNames
            - repositoryRef
            - targetNamespace
            type: object
          status:
            nullable: true
            properties:
              lastRunName:
                description: Name of the last scheduled `ProteusBackup` run created by the controller.
                nullable: true
                type: string
              lastScheduleTime:
                description: When the controller last spawned a scheduled run (RFC3339 UTC).
                nullable: true
                type: string
              message:
                nullable: true
                type: string
              nextRunAt:
                description: Next scheduled fire time (RFC3339 UTC), when a schedule is set and not paused.
                nullable: true
                type: string
              phase:
                enum:
                - Ready
                - Invalid
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: ProteusBackupPolicy
        type: object
    served: true
    storage: true
    subresources:
      status: {}
