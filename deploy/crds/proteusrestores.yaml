apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: proteusrestores.proteus.io
spec:
  group: proteus.io
  names:
    categories: []
    kind: ProteusRestore
    plural: proteusrestores
    shortNames:
    - prestore
    singular: proteusrestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.backupRef
      name: Backup
      type: string
    - jsonPath: .spec.targetNamespace
      name: TargetNS
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ProteusRestoreSpec via `CustomResource`
        properties:
          spec:
            properties:
              backupNamespace:
                nullable: true
                type: string
              backupRef:
                type: string
              includeResources:
                nullable: true
                type: string
              overwrite:
                default: false
                type: boolean
              snapshotId:
                nullable: true
                type: string
              targetNamespace:
                type: string
            required:
            - backupRef
            - targetNamespace
            type: object
          status:
            nullable: true
            properties:
              assignedNode:
                description: Node that should run (or ran) an agent-plane restore.
                nullable: true
                type: string
              completedAt:
                nullable: true
                type: string
              dataPlane:
                description: Bulk I/O path used for this run (`exec` | `agent`).
                enum:
                - exec
                - agent
                nullable: true
                type: string
              durationSeconds:
                description: Wall-clock duration of a successful run, in seconds.
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              lastBytes:
                description: Bytes written across restored volumes.
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              message:
                nullable: true
                type: string
              phase:
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                nullable: true
                type: string
              progressPercent:
                format: uint8
                minimum: 0.0
                nullable: true
                type: integer
              restoredSnapshotId:
                nullable: true
                type: string
              startedAt:
                description: When the current/last run started (RFC3339).
                nullable: true
                type: string
              throughputBytesPerSec:
                description: Approximate throughput (`lastBytes / durationSeconds`).
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: ProteusRestore
        type: object
    served: true
    storage: true
    subresources:
      status: {}
