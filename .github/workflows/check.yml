name: check

on:
  pull_request:
  push:
    branches: [main, proteus-fork-validation]
    tags: ["v0.0.0-craftingtech-workspace-validation.*"]

concurrency:
  group: check-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: just check
    runs-on: ubuntu-latest
    env:
      BORINGCACHE_ENABLED: ${{ vars.BORINGCACHE_ENABLED == 'true' && (secrets.BORINGCACHE_RESTORE_TOKEN != '' || (github.event_name != 'pull_request' && secrets.BORINGCACHE_SAVE_TOKEN != '')) && 'true' || 'false' }}
    steps:
      - uses: actions/checkout@v4

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends binaryen

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.91.1
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Cache Rust with BoringCache
        if: env.BORINGCACHE_ENABLED == 'true'
        uses: boringcache/one@62d929848c7c2f600dc721a849794f2cdca9c6cc # v1.16.7
        with:
          trust-policy: auto
          setup: none
          mode: sccache
          metadata-hints: workflow=check
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_SAVE_TOKEN: ${{ github.event_name != 'pull_request' && secrets.BORINGCACHE_SAVE_TOKEN || '' }}

      - name: Cache Rust with GitHub Actions
        if: env.BORINGCACHE_ENABLED != 'true'
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: check

      - name: Install just
        uses: extractions/setup-just@v3

      - name: Install dioxus-cli
        uses: taiki-e/cache-cargo-install-action@v2
        with:
          tool: dioxus-cli@0.7.9
          locked: true

      - name: just check
        run: just check

  kustomize:
    name: kustomize build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Render install package
        run: kubectl kustomize deploy/overlays/default > /tmp/proteus.yaml

      - name: Sanity check rendered manifests
        run: |
          grep -q 'kind: Deployment' /tmp/proteus.yaml
          grep -q 'ghcr.io/craftingtech/proteus-controller:' /tmp/proteus.yaml
          grep -q 'proteusbackups.proteus.io' /tmp/proteus.yaml

  # Sticky conversation comment so CI result is visible in the PR timeline
  # (Actions check runs alone are easy to miss without required status checks).
  notify:
    name: PR status comment
    if: always() && github.event_name == 'pull_request'
    needs: [check, kustomize]
    runs-on: ubuntu-latest
    steps:
      - name: Post or update CI comment
        uses: actions/github-script@v7
        with:
          script: |
            const marker = '<!-- proteus-ci-status -->';
            const check = '${{ needs.check.result }}';
            const kustomize = '${{ needs.kustomize.result }}';
            const ok = check === 'success' && kustomize === 'success';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const body = [
              marker,
              '### CI — `check`',
              '',
              '| Job | Result |',
              '| --- | --- |',
              `| just check | \`${check}\` |`,
              `| kustomize build | \`${kustomize}\` |`,
              '',
              ok
                ? '✅ CI green.'
                : '❌ CI failed — open the workflow run for logs.',
              '',
              `[Workflow run](${runUrl})`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100,
            });
            const existing = comments.find((c) => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
