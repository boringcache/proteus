# Proteus brand assets

| File | Role |
| --- | --- |
| `mark.svg` | Filled heptagon (Kube-inspired) + shield; `fill=currentColor` on heptagon, white shield |
| `wordmark.svg` | Letterforms from name kit (heptagon O); `fill=currentColor` |
| `../favicon.svg` | Same as mark |

Theme the mark by setting CSS `color` on `.brand-mark` (wired to `--accent` = `#3069de`). Wordmark uses `--text`.

Accent **kube** (`#3069de`) is locked product brand — compare switcher removed after validation.
